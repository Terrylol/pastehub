# PasteHub 产品方案书（MVP）

> 版本：0.2 · 日期：2026-08-16 · 状态：方案已确认，待技术实现

## 1. 一句话定义

**PasteHub 是一个无需登录、会自动消失的跨设备临时收件箱。**

用户在旧设备放入一段文本、截图或小文件，在新电脑打开链接、扫二维码或输入六码即可取走；需要连续工作时，多台设备可加入同一个临时空间并实时看到新内容。

## 2. 要解决的问题

常见需求是把命令、链接、AI 回复、验证码以外的普通字符串、截图或小文件，在手机、个人电脑、临时电脑间移动。现有路径都偏重：

| 路径 | 问题 |
| --- | --- |
| 给自己发消息 / 邮件 | 需要登录，会留下长期记录 |
| 原生跨设备剪贴板 | 受系统、账号和设备生态限制 |
| 网盘 | 对一段字符串而言步骤太多 |
| 在线粘贴板 | 常只有单次链接，或服务端可读内容 |

PasteHub 的原则：**先能在 10 秒内取到，再把内容自动清掉。**

## 3. 市场与竞品结论

| 产品 | 已验证优势 | 缺口 / 启示 |
| --- | --- | --- |
| tmps.fyi | 临时空间、跨设备实时更新、可选密码；最后设备离开约 10 分钟后清理 | 明确不是端到端加密；适合做「空间」参考 |
| ClipShare | 短码、二维码、链接、浏览器本地草稿、一次查看的 Snap | 更偏单次交接；适合做「取件」参考 |
| CopyPaste.me | QR/邀请码配对，网页端端到端加密，不存储内容 | 需要两端同时在线，缺少可短暂留存的收件箱 |
| Yopass | 浏览器本地加密、一次性秘密链接、开源 / 可自托管 | 很适合密钥，不是多条跨设备临时空间 |

**产品机会：** 把「实时临时空间」和「低摩擦取件」合在一起，服务于不愿登录、只想快速把内容拿到另一台设备上的普通用户。

本结论基于各产品公开说明，未做第三方安全审计验证；安全承诺上线前必须经独立审计后再宣传。

## 4. 目标用户与优先场景

### 目标用户

- 在手机、家用电脑、工作电脑之间切换的普通用户。
- 需要偶尔借用或新装一台电脑，但不愿登录个人账号的人。

### 优先场景

1. **新电脑取一段字符串**：电脑复制命令 → 生成二维码 / 六位码 → 新电脑打开 PasteHub → 复制。
2. **手机与电脑连续转内容**：建一个空间，丢入链接、截图、文本；两端实时出现。
3. **临时传敏感文本**：生成一次性 Secret，查看一次后销毁。

不把「自动同步系统剪贴板」纳入 Web MVP：网页受浏览器权限限制，不能在后台读取系统剪贴板。未来可用浏览器扩展补足。

## 5. 产品结构

```text
本地草稿（只存当前浏览器）
 ├── 快速投递：一条内容 → 短码 / QR / 链接 → 10 分钟过期
 ├── 临时空间：多条内容 → 多设备实时同步 → 最后离开后 10 分钟清理
 └── Secret：一条文本 → 一次查看 → 立即销毁
```

### MVP 功能范围

| 模块 | MVP 要做 | 暂不做 |
| --- | --- | --- |
| 快速投递 | 文本、链接、图片；六码、QR、复制链接；手动删除与 10 分钟过期 | 账号、长期历史、文件夹、文件上传 |
| 临时空间 | 第二阶段：创建/加入；文本、图片；实时列表；最后离开后 10 分钟删除 | 多人编辑、评论、权限角色、文件上传 |
| Secret | 第二阶段：一次读取；最多存活 10 分钟 | 大文件、阅读回执、企业审计 |
| 本地体验 | 粘贴、复制、图片上传 / 粘贴 | 原生 App、后台剪贴板监听、PWA |

### 首发版本的产品决策

- **唯一主路径：快速投递。** 首页不展示「空间」和「Secret」。
- **内容类型：文本与图片。** 文本上限 10,000 字符；图片上限 10 MB。
- **保存规则：创建后 10 分钟自动删除。** 不因用户是否在线而延长。
- **取件方式：二维码、分享链接、六码。** 三者对应同一条投递。
- **不需要登录。** 创建者在当前浏览器可删除自己创建的投递；取件者只能读取。
- **第一版不是端到端加密。** 首页和创建结果页持续展示敏感内容提醒。

## 6. 关键用户流程

### A. 快速投递（默认入口）

1. 用户打开首页，粘贴内容。
2. 点击「发送」，页面展示 6 位取件码、二维码与复制链接。
3. 新设备扫描二维码、打开链接或输入六码。
4. 内容展示；用户点击「复制」。
5. 到期、手动删除或 Secret 被读取后，内容不可恢复。

### B. 临时空间（第二阶段）

1. 用户点击「创建空间」，系统生成易读空间名与邀请二维码。
2. 另一设备通过二维码/链接进入；页面显示「已连接」。
3. 任意一端添加条目，其他已连接设备实时出现。
4. 所有人离开后启动清理倒计时；倒计时内再次进入则恢复。

## 7. 安全与隐私设计（上线门槛）

### 承诺与边界

- **不需要账号**，但匿名不等于不可追踪：仍需要最低限度的反滥用日志与速率控制。
- 第一版通过 HTTPS 传输，并在服务端临时保存内容；不宣称端到端加密，也不建议用户传输密码、私钥、验证码或其他敏感内容。
- 系统不承诺阻止接收者复制、截图或拍照；「阅后即焚」只控制服务端可访问性。

### 第一版实现与后续演进

- 第一版强制 TLS、严格 CSP，且不接入广告或读取内容的第三方分析脚本。
- 分享码使用足够的随机性，避免连续编号；链接和码都在到期/删除后立刻失效。
- 服务端日志不得记录正文、图片二进制或完整分享链接。
- 后续的 Secret / 加密模式：为每个空间或投递生成随机 256-bit 内容密钥，用 AES-GCM 在浏览器加密；密钥放在 URL `#fragment`，不发给服务端。

### 反滥用与成本控制

- 匿名接口按 IP / 会话做速率和并发限制，设置验证码为触发式而非默认。
- MVP 限制单文件 10 MB、空间总量 30 MB、最长 24 小时；病毒扫描仅能针对密文以外的上传风险，不能承诺内容审查。
- 设置举报入口、自动到期删除、对象存储生命周期规则与滥用处理流程。

## 8. 成功指标

| 指标 | MVP 目标 |
| --- | --- |
| 首次成功取件时间 | 中位数 < 20 秒 |
| 创建后成功在第二设备打开的比例 | > 70% |
| 二次使用率（7 天） | > 20% |
| 内容超期删除成功率 | 100%（监控告警） |
| 明文进入服务端日志 | 0（审计项） |

### 首发验收标准

- 访客无需注册、无需 Cookie 弹窗，即可完成创建和取件。
- 文本创建到拿到取件码的 P95 时间不超过 1 秒；图片完成上传后不超过 3 秒（10 MB 以内，正常网络）。
- 取件码、链接和二维码均可从手机/桌面端完成取件。
- 已删除和已过期的内容，读取接口必须返回不可恢复的失效状态。
- 首页首屏只有「发送」与「取件」两种可点击主动作。

## 9. 交付路线

### 第 1 阶段：验证（约 1–2 周）

- 文本和图片的快速投递：QR、短码、自动过期。
- 收集 10–20 位真实用户的「第二设备取件是否足够快」反馈。

### 第 2 阶段：可用 MVP（约 2–3 周）

- 临时空间、实时同步、Secret、删除与限流。
- 基础监控、错误追踪、生命周期删除校验。

### 第 3 阶段：验证留存

- PWA、浏览器扩展、空间固定入口（仍不需要账号）。
- 只在确认用户持续使用后，再增加端到端加密模式、自托管版或文件传输。

## 10. 暂定命名与定位文案

**PasteHub**

> Paste here. Pick up anywhere. Gone when you’re done.
>
> 不登录，跨设备，自动消失。

## 11. 首页设计原则

首页采用「早期百度」式的极简结构：大量留白、居中单任务、没有信息流和功能卡片。用户第一次看到页面时，应该立刻理解为「把内容放这里，然后去另一台设备取」。

```text


                         PasteHub
                  在这里放下，去另一台设备取

              [ 粘贴文本，或拖入 / 粘贴一张图片          ]

                        [ 发送 ]

              无需登录 · 10 分钟后自动删除


                  [ 输入取件码 ]  [ 取件 ]

```

- 首页只保留两个动作：**发送**与**取件**；不在首屏展示「空间」「Secret」等概念。
- 发送后进入结果页，突出二维码、六码和「复制链接」，其他内容全部后置。
- 文案用普通人的语言，避免「剪贴板同步」「端到端」等技术术语。
- 空间和 Secret 均放在首发以后，或作为结果页的次级入口，避免干扰第一次使用。

## 12. 本次评审结论

1. **首发支持文本与图片。** 不做文件。
2. **第一版不做端到端加密。** 必须清晰提示不应用于密码、验证码、私钥等敏感内容。
3. **首发面向普通用户。** 首页优先极简，避免技术术语与复杂模式。
4. 待确认：临时空间是否与快速投递同时首发，还是验证快速投递后再发布。

## 13. 参考资料

- [tmps.fyi：临时跨设备空间、密码与删除策略](https://tmps.fyi/)
- [ClipShare：短码、QR、临时交接与 Snap](https://clipshare.cc/en)
- [CopyPaste.me：浏览器端 E2E 加密和 QR 配对](https://copypaste.me/)
- [Yopass：浏览器本地加密的一次性秘密分享](https://yopass.se/)
- [Tor Project：OnionShare 的无账号直接传输模型](https://support.torproject.org/about-tor/using-and-sharing/onionshare/)
