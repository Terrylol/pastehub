## 1. 运行时数据服务

- [x] 1.1 完善 Docker Compose 的 MySQL、Redis 命名卷、健康检查、网络及开发凭据配置。
- [x] 1.2 增加 Spring Boot 的 MySQL 生产配置、Redis 连接配置和环境变量说明；保留 H2 测试/显式开发配置。
- [x] 1.3 验证 Flyway 能在新的 MySQL 实例创建文本和图片投递所需表结构。

## 2. Redis IP 限流

- [x] 2.1 引入 Redis 客户端与可配置的限流属性：动作配额、窗口和可信代理来源。
- [x] 2.2 实现安全的客户端 IP 解析和 Redis 原子固定窗口计数；Redis 不可用时拒绝受保护写入。
- [x] 2.3 在文本创建/删除、图片初始化/发布/删除接口前应用动作级限流，并返回统一的 429、错误码与 Retry-After。

## 3. 验证与运行文档

- [x] 3.1 为不同 IP、超限无状态变更、窗口恢复、转发头边界及 Redis 不可用场景添加自动化测试。
- [x] 3.2 使用 Docker MySQL 验证应用重启后仍可读取未过期文本和图片投递。
- [x] 3.3 更新中文 README，说明本地启动、生产环境变量、可信代理和限流默认值。
- [x] 3.4 运行后端测试、前端构建、Docker 冒烟验证及 OpenSpec 严格校验。
