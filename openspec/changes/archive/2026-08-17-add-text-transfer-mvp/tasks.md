## 1. 后端持久化与 API

- [x] 1.1 为临时文本、取件码、过期时间和哈希删除权限添加兼容 H2/MySQL 的数据库迁移与投递持久化模型。
- [x] 1.2 在 `/api/v1/transfers` 下实现文本投递创建、读取、取件码解析、删除和读取时强制过期行为。
- [x] 1.3 添加过期投递定时清理、保护隐私的错误处理和禁止缓存的响应头。

## 2. 后端验证

- [x] 2.1 为有效和无效创建、按标识和取件码读取、过期、删除幂等和无效删除权限添加自动化 API 覆盖。

## 3. 浏览器投递体验

- [x] 3.1 为首页、创建结果、取件和不可用状态添加 Vue Router 路由与类型化 API 客户端。
- [x] 3.2 将极简首页接入真实的文本创建和取件码解析，并包含校验和敏感内容提醒。
- [x] 3.3 实现创建结果和取件页面：支持二维码/链接/文本复制降级、过期倒计时、会话范围内的删除令牌和不可用处理。

## 4. 端到端验证

- [x] 4.1 运行后端测试、构建前端、校验 OpenSpec 变更；如实现契约需要，更新本地启动文档。
